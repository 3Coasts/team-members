# Instructions

* Make sure "Features" module is added to the site and enabled.
* Enable the "Team Members" feature module.
* Go to admin/structure/features and enable the feature.
* Create a page and add the blocks.